# data/

Place the labeled dataset here as `labeled_dataset.csv` with two columns:

- `text` — the post body
- `label` — one of `critique`, `observation`, `inquiry`

This is the annotated version of `truefilm_raw.csv` produced by the notebook's
Reddit-scraping cell.
